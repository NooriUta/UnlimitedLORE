# LORE v1.0.46 — Открытые вопросы, дочерние решения, мультипроект и чистка корпуса

Крупный релиз слоя знаний LORE: реестр открытых вопросов, полноценная работа с решениями как дочерними ADR, доработка фильтров/UX, привязка вопросов и решений к нескольким проектам/компонентам, важный баг-фикс статусов и большая чистка корпуса.

## ✨ Реестр открытых вопросов (ОВ) — ADR-020/021
- Новый раздел «Вопросы» (после «Спринты», перед «ADR»): статусы open/deferred/closed/dropped, приоритет, срок (due_date), владелец; **красная подсветка просроченных**.
- Создание и редактирование вопросов прямо в реестре; компонент — выбор из списка (datalist).
- Секция **«Открытые вопросы этого ADR»** в паспорте (обход `RAISED_IN`).
- Обратное извлечение 40 вопросов из корпуса → вершины + рёбра ANSWERS/RAISED_IN.

## 🧩 Решения как дочерние ADR — ADR-019
- Редактирование решений **в контексте паспорта ADR** (+решение, ✎), компонент-datalist.
- Борд «Решения»: фильтр **«Под ADR / Независимые»**, иконка-маркер независимых, **поиск и по родительскому ADR**, заметный тумблер «Независимые N».
- Раздел «Решения» убран из главной навигации → подвкладка **«Аналитика | Решения»**.

## 🔎 UX фильтров и карточек ADR
- Сворачиваемые дименсии **Компонент/Тег**, фильтр **«без компонента»**, **сортировка + период** перенесены под кнопку «+ Новый ADR».
- Название ADR **на отдельной строке** (в списке и в паспорте).
- **MD-экспорт ADR** теперь включает дочерние решения (DES) и открытые вопросы.

## 🔗 Мультипроект / мультикомпонент — T43 (backend + MCP)
- Вопросы и решения могут принадлежать **нескольким проектам** (`BELONGS_TO_PROJECT`) и **нескольким компонентам** (`BELONGS_TO`) — как задачи.
- Эндпоинты `question|decision/component|project` (add/remove); MCP `question_link` +rel `project`, новый инструмент **`decision_link`** (component|project).
- Слайсы `decisions`/`decisions_of_adr`/`open_questions`/`questions_of_adr` отдают `components[]`/`projects[]`.
- _UI-мультипикеры и фильтры по связанным — следующий шаг (post-deploy)._

## 🐛 Исправления
- **`status_set` больше не врёт об успехе на неизвестном id**: добавлен гард на существование вершины — при отсутствии совпадения возвращается **404**, а не тихий no-op с ok:true (и без вставки орфан-Hist). Раньше передача короткого `task_id` вместо полного `task_uid` молча ничего не меняла.

## 🧹 Чистка корпуса (данные, живая БД)
- 22 ADR: вычищены вклеенные блоки «Мигрированные решения из DECISIONS_LOG»; забэкфиллены 96 рёбер `DECIDED_IN`.
- **31 псевдо-ADR `D-NNN` растворены обратно в `KnowDecision`** и привязаны к 14 настоящим родителям (SKULD/ANVIL/YGG/HND/CI/OBS/MT/SVC/MIMIR) — namespace ADR не раздут.
- Удалён 51 орфан-`KnowTaskHist` (накопились из-за бага status_set); бэкфилл компонент-рёбер (41 вопрос); 6 стек-решений привязаны к ADR-TS-MODULES-STACK.

## Deploy notes
Требуется пересборка **backend + MCP** (эндпоинты/слайсы T43, фикс `status_set`). Фронтенд — статика. После деплоя: доделать UI-мультипикеры T43 и фильтры по связанным.

---
🤖 Generated with [Claude Code](https://claude.com/claude-code)
